server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;
    # Necessário para React Router funcionar
    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api {
    proxy_pass http://demigtools-backend:3000;
}

}